{
	"name": "anthill-rules-tests",
	"private": true,
	"type": "module",
	"description": "Security-rules tests. Kept out of the React Native package.json so the Firebase web SDK never enters the app's dependency tree.",
	"scripts": {
		"test": "cd ../.. && firebase emulators:exec --only firestore \"node --test tools/rules-tests/*.test.mjs\""
	},
	"devDependencies": {
		"@firebase/rules-unit-testing": "^3.0.4",
		"firebase": "^10.13.2"
	}
}
