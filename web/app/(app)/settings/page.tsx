import { Placeholder } from "@/components/Placeholder";

export default function SettingsPage() {
	return (
		<Placeholder
			eyebrow="Company"
			title="Company settings"
			note="Feature toggles, work-week start, the custom form builder, and the checklist/package/label libraries. Coming next."
		/>
	);
}
