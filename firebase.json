{
	"firestore": [
		{
			"database": "(default)",
			"rules": "firestore.rules"
		},
		{
			"database": "test",
			"rules": "firestore.rules"
		}
	],
	"emulators": {
		"firestore": {
			"port": 8080
		},
		"singleProjectMode": true,
		"ui": {
			"enabled": false
		}
	},
	"storage": {
		"rules": "storage.rules"
	}
}
