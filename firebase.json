{
	"firestore": [
		{
			"database": "(default)",
			"rules": "firestore.rules",
			"indexes": "firestore.indexes.json"
		},
		{
			"database": "test",
			"rules": "firestore.rules",
			"indexes": "firestore.indexes.json"
		}
	],
	"emulators": {
		"firestore": {
			"port": 8080
		},
		"singleProjectMode": true,
		"ui": {
			"enabled": false
		}
	},
	"storage": {
		"rules": "storage.rules"
	}
}
